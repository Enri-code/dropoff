name: dropoff
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  either_dart: ^1.0.0
  equatable: ^2.0.5
  flutter_bloc: ^8.1.6
  flutter_screenutil: ^5.9.3
  # fluttertoast: ^8.2.6
  hive_flutter: ^1.1.0
  intl: ^0.19.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
